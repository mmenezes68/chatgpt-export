\documentclass{abntex2}
\usepackage[a4paper,top=3cm,bottom=2cm,left=3cm,right=2cm]{geometry}

% Fontes
\usepackage{fontspec}
\setmainfont{Times New Roman}[
  BoldFont      = Times New Roman Bold,
  ItalicFont    = Times New Roman Italic,
  BoldItalicFont= Times New Roman Bold Italic
]
\usepackage{microtype}

% Idioma (via Pandoc -V lang=...)
\usepackage{polyglossia}
$if(lang)$\setdefaultlanguage{$lang$}$else$\setdefaultlanguage{brazil}$endif$

% Metadados (Pandoc)
\titulo{$title$}
\autor{$author$}
\instituicao{$if(institution)$$institution$$else$Universidade de São Paulo$endif$}
\local{$place$}
\data{$year$}

% Capa (USP/ICB)
\makeatletter
\renewcommand{\imprimircapa}{%
  \begin{capa}
    \centering
    {\fontsize{14}{16}\selectfont\textbf{Universidade de São Paulo}\par}
    {\fontsize{12}{14}\selectfont Instituto de Ciências Biomédicas\par}
    \vspace*{2.2cm}
    {\fontsize{12}{14}\selectfont \MakeUppercase{\imprimirautor}\par}
    \vfill
    {\fontsize{14}{16}\selectfont\textbf{\imprimirtitulo}\par}
    \vfill
    {\fontsize{12}{14}\selectfont Orientador: Prof. Dr. Walter Chesnut\par}
    \vfill
    {\fontsize{12}{14}\selectfont\imprimirlocal\par}
    {\fontsize{12}{14}\selectfont \imprimirdata\par}
  \end{capa}%
}
\makeatother

% Sumário (tamanho uniforme + pontilhado denso)
\makeatletter
\makeatother

\begin{document}
% === FIXA NUMERAÇÃO DAS SEÇÕES (sem capítulo) ===
\usepackage{chngcntr}
\counterwithout{section}{chapter}
\counterwithout{subsection}{chapter}
\counterwithout{subsubsection}{chapter}
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{\arabic{section}.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\arabic{section}.\arabic{subsection}.\arabic{subsubsection}}
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

\pretextual
\imprimircapa
\imprimirfolhaderosto*

\pdfbookmark[0]{\contentsname}{toc}
\tableofcontents*
\cleardoublepage
\listoffigures*
\cleardoublepage
\listoftables*
\cleardoublepage

$body$
\textual

$if(bibliography)$
\bibliographystyle{abntex2-alf}
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}
$endif$

\end{document}
% --- Idioma dinâmico (PT/EN) ---
\usepackage{polyglossia}
\ifdefined\ABNTlang
  \setdefaultlanguage{\ABNTlang}
\else
  \setdefaultlanguage{brazil}
\fi
% --- Guards para comprimentos do tocloft (caso não existam) ---
\makeatletter
\makeatother
% --- Forçar seção como nível 1 (sem capítulo 0) ---
% --- Garantir níveis no sumário até subseção ---
% --- Níveis de numeração e sumário (sem depender de tocloft) ---
\AtBeginDocument{%
}
%%NUMBLOCK%%
\AtBeginDocument{%
}
% --- Numeração sem capítulo: 1, 1.1, 1.1.1 (nada de 0.1) ---
\AtBeginDocument{%
}
% --- Numeração de seções sem capítulo (evita "0.1") ---
\makeatletter


% memoir: usa exatamente os 'the*' acima em cabeçalhos/ToC

\makeatother
% --- Numeração sem capítulo ("0." removido) ---
\AtBeginDocument{%
}
% --- Corrigir sumário: remover prefixo "0." das seções ---
\makeatletter
\AtBeginDocument{%
  % remove vínculo de seção a capítulo
  % redefine como aparecem no TOC
  \renewcommand{\numberline}[1]{#1\hspace{0.7em}}
  % garante profundidade
}
\makeatother
% --- FIX: tirar "0." do sumário (section como nível raiz) ---
\usepackage{etoolbox}
\makeatletter
\AtBeginDocument{%
}
% Garante o formato correto exatamente antes do \tableofcontents
\pretocmd{\tableofcontents}{%
}{}{}
\makeatother
% --- FIX FINAL: retirar prefixo "0." das seções ---
\makeatletter
\AtBeginDocument{%
  % remove a associação das seções ao contador de capítulo
  % força numeração limpa
  % profundidade
}
\makeatother
% --- FIX: remover prefixo "0." da numeração das seções ---
\makeatletter
\AtBeginDocument{%
  % Zera capítulos e remove ligação entre chapter e section
  \setcounter{chapter}{0}

  % Numeração limpa

  % Profundidade
}
\makeatother
% --- Numeração de seção no topo (sem capítulo) ---
\usepackage{chngcntr}
\makeatletter
\AtBeginDocument{%
  \counterwithout{section}{chapter}%
  \counterwithout{subsection}{chapter}%
  \counterwithout{subsubsection}{chapter}%
  \renewcommand{\thesection}{\arabic{section}}%
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%
  \setcounter{secnumdepth}{3}%
  \setcounter{tocdepth}{3}%
  \setsecnumformat{\csname the#1\endcsname\quad}%
}
\makeatother
% --- Corrigir numeração: remover prefixo "0." ---
\makeatletter
\setsecnumdepth{section}        % seção é o nível mais alto
\setcounter{secnumdepth}{3}     % numerar até subsubseção
\setcounter{tocdepth}{3}        % incluir até subsubseção no sumário
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
\makeatother
% --- Numeração sem capítulo (teste validado) ---
\makeatletter
\setsecnumdepth{section}        % seção = nível mais alto
\setcounter{secnumdepth}{3}     % numerar até subsubseção
\setcounter{tocdepth}{3}        % incluir até subsubseção no sumário
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
\makeatother
% --- Forçar seção como nível 1 (sem capítulo fantasma) ---
\makeatletter
\counterwithout{section}{chapter}
\counterwithout{subsection}{chapter}
\counterwithout{subsubsection}{chapter}
\setcounter{section}{0}  % garante que comece do 1
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{\thesection.\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}
\makeatother
% === Fix: numeração sem capítulo (elimina 0.x) ===
\usepackage{chngcntr}
\AtBeginDocument{%
  \counterwithout{section}{chapter}%
  \counterwithout{subsection}{chapter}%
  \counterwithout{subsubsection}{chapter}%
  \setsecnumdepth{section}% memoir: topo = section
  \setcounter{secnumdepth}{3}% até subsubsection
  \setcounter{tocdepth}{3}% até subsubsection no sumário
  \renewcommand{\thesection}{\arabic{section}}%
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%
}

% === NUMERAÇÃO SEM CAPÍTULO (ABNTEX2/MEMOIR) ===
\usepackage{chngcntr}
\AtBeginDocument{%
  \counterwithout{section}{chapter}%
  \counterwithout{subsection}{chapter}%
  \counterwithout{subsubsection}{chapter}%
  \setsecnumdepth{section}% seção = nível superior
  \setcounter{secnumdepth}{3}% numera até subsubsection
  \setcounter{tocdepth}{3}% inclui até subsubsection no sumário
  \renewcommand{\thesection}{\arabic{section}}%
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%
}

% === NUMERAÇÃO SEM CAPÍTULO (fix 0.x no ToC) ===
\usepackage{chngcntr}
\makeatletter
\AtBeginDocument{%
  % Remove dependência de capítulo
  \counterwithout{section}{chapter}%
  \counterwithout{subsection}{chapter}%
  \counterwithout{subsubsection}{chapter}%
  % Remove prefixo de capítulo na representação (evita "0.")
  \renewcommand{\thechapter}{}%
  % Profundidade e formato
  \setsecnumdepth{section}%
  \setcounter{secnumdepth}{3}%
  \setcounter{tocdepth}{3}%
  \renewcommand{\thesection}{\arabic{section}}%
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%
}
\makeatother

% === AJUSTE FINAL DE NUMERAÇÃO (remove prefixo 0.) ===
\AtBeginDocument{%
  % Seções independentes de capítulos
  \counterwithout{section}{chapter}%
  \counterwithout{subsection}{chapter}%
  \counterwithout{subsubsection}{chapter}%

  % Profundidade
  \setsecnumdepth{section}%
  \setcounter{secnumdepth}{3}%
  \setcounter{tocdepth}{3}%

  % Formato da numeração
  \renewcommand{\thesection}{\arabic{section}}%
  \renewcommand{\thesubsection}{\thesection.\arabic{subsection}}%
  \renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}%

  % Corrige início (sem 0.)
  \setcounter{section}{0}%
}
